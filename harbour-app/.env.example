# Optional: enables Gemini chat. The app has a rule-based fallback without it.
GEMINI_API_KEY=

# Optional: enables durable shared storage. Configure both or neither.
# The project URL is public configuration; keep the matching key private.
SUPABASE_URL=https://etpydjcmyjhqmiblkofr.supabase.co
SUPABASE_KEY=
